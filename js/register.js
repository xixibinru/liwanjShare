window.addEventListener('load',function () {
    var register = new Vue({
        el: '#register',
        data: {
            a: 1,
        }
    });
    var footer = new Vue({
        el: '#footer'
    });
});
